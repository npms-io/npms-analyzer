'use strict';

describe('mergePackageJson', () => {
    it('should merge the files, prefering the passed one');

    it('should merge the files, prefering the downloaded one');

    it('should deal with broken downloaded package.json files');
});
