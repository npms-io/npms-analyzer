'use strict';

describe('index', () => {
    it('should find an appropriate downloader, passing in the correct options');

    it('should call downloader with the temporary folder');

    it('should delete the temporary folder on failure');
});
